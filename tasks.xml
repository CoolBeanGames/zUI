<?xml version="1.0" encoding="utf-8"?>
<tasks project="zUI" nextIndex="55">
  <task id="ZU-46">
    <name>do NOT merge this branch until i tell you otherwise!</name>
    <prompt>Release the verified refactor work, merge it into main, push GitHub, publish the next GitHub release, and close all tasks.</prompt>
    <requirements>Full test and release builds pass; refactor is merged and pushed to main; the next numbered GitHub release is published with patch notes; no open task records remain.</requirements>
    <inProgress>true</inProgress>
    <done>false</done>
    <bug>false</bug>
    <error>false</error>
    <errorMessage></errorMessage>
    <lockKey>zr9K4m2Q7x</lockKey>
    <locked>false</locked>
    <archived>false</archived>
    <blockedBy></blockedBy>
    <dateStarted></dateStarted>
    <dueDate></dueDate>
    <commit>true</commit>
    <build>true</build>
    <release>true</release>
    <merge>true</merge>
    <branch>refactor</branch>
    <tags></tags>
    <notes></notes>
    <filesChanged></filesChanged>
    <image></image>
    <subtasks>
      <subtask done="true">Verify the refactor branch, determine the next build/release identifier, and prepare patch notes</subtask>
      <subtask done="true">Run the complete test and release builds and smoke-check the release executables</subtask>
      <subtask done="false">Merge refactor into main without discarding history and push main to GitHub</subtask>
      <subtask done="false">Create and push the release tag and publish the GitHub release with patch notes</subtask>
      <subtask done="false">Close all task records, record release metadata, commit bookkeeping, and push final state</subtask>
      <subtask done="true">Audit and update instructions.txt so branch, verification, release, and task-closing guidance matches the current workflow</subtask>
    </subtasks>
  </task>
</tasks>